#include <stdio.h>
int
Main ()
{
  int first_iteration;
  int t;
  int x;
  int x;
  int p;
  int flag = 1;
  int numbers;
  scanf ("%d", &t);
  while (t--)
    {
      scanf ("%d %d %d", &x, &x, &p);
      printf ("Case #%d:", flag++);
      for (numbers = 1; numbers <= x; ++numbers)
	{
	  printf (" %d", numbers);
	}
      printf ("\n");
    }
  return 0;
}
