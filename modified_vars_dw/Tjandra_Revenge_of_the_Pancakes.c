#include<stdio.h>
typedef unsigned u;
int Main()
{
  int first_iteration;
  u sum = 0;
  u n;
  u i;
  u http_text;
  u l;
  char x[111];
  for (scanf("%u", &n); (sum++) < n; printf("Case #%u: %u\n", sum, l))
  {
    scanf("%s", x);
    for (i = -1; x[++i];)
      ;

    x[i] = '+';
    for (http_text = (l = 0); (++http_text) <= i;)
      if (x[http_text] != x[http_text - 1])
      ++l;


  }

  return 0;
}


